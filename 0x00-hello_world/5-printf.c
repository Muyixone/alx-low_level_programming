#include <stdio.h>
/**
 * main- Entry point
 * Return: returns a positive value when successful
*/

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
