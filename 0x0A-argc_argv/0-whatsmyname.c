#include <stdlib.h>
#include <stdio.h>
#include "main.h"
/**
  * main - The main function that accepts commnand line arguments.
  * @argc: Integer parameter that counts the number of arguments
  * past in the command line.
  * @argv: A string of character array past
  * to the command line.
  *
  * Return: 0 success.
*/

int main(__attribute__((unused)) int argc, char *argv[])
{
	printf(" %s \n", argv[0]);
	return (0);
}
